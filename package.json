{
  "name": "notion-career-agent",
  "version": "1.0.0",
  "description": "AI career agent for Notion — scans jobs, tailors resumes, tracks applications",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "deploy": "ntn workers deploy",
    "dev": "ntn workers dev"
  },
  "dependencies": {
    "@notionhq/workers": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
